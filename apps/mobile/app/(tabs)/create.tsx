// Placeholder route for the center "+" tab. The tab bar intercepts the press
// (see _layout.tsx CenterButton) and opens a quick-create sheet, so this screen
// is never actually shown.
export default function CreatePlaceholder() {
  return null;
}

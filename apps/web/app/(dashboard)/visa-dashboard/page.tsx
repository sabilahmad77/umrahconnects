import { VisaDashboard } from '@/components/compliance/visa-dashboard';
export const metadata = { title: 'Visa Agency — Dashboard' };
export default function VisaDashboardPage() {
  return <VisaDashboard />;
}

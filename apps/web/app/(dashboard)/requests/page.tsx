import { RequestsView } from '@/components/requests/requests-view';

export const metadata = { title: 'My Marketplace Requests' };

export default function RequestsPage() {
  return <RequestsView />;
}

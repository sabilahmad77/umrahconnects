import { AssignmentsList } from '@/components/transport/assignments-list';
export const metadata = { title: 'Transport Assignments' };
export default function AssignmentsPage() {
  return <AssignmentsList />;
}

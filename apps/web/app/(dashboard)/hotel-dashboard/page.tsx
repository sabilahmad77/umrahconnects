import { HotelDashboard } from '@/components/hotels/hotel-dashboard';
export const metadata = { title: 'Hotel — Dashboard' };
export default function HotelDashboardPage() {
  return <HotelDashboard />;
}

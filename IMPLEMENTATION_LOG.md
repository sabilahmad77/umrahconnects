# IMPLEMENTATION_LOG — Umrah Connect autonomous loop

Format: `ISSUE · files · verified · RESULT`

- STEP-0 · STACK_NOTES.md · Verified: /proxy-api = Next rewrite→NestJS in-repo; auth = JWT localStorage + client gate; canonical enums from prisma; discrepancies logged (payments endpoint EXISTS, packages API EXISTS, real `#` links = login footer ×3 + social icons ×5; pilgrim DTO enum ≠ DB enum) · PASS
- FIX-01 · auth/dto/register.dto.ts (PUBLIC_SIGNUP_ROLES allow-list + roleInterest), auth.service.ts (403 ForbiddenException + audit warn), app/signup/page.tsx (removed Super Admin tile, sends roleInterest) · Verified: direct API inject admin/superadmin → 403; operator/none → 201; UI offers only 6 self-service roles, no Super Admin · PASS
- FIX-02 · lib/api.ts (shared coalesced refreshAccessToken singleton; interceptor uses it, redirects with returnTo only on genuine failure), components/providers/auth-provider.tsx (refresh-before-bounce on expired access token at hard nav; returnTo; demo-session keep), app/(auth)/login/page.tsx (honor ?returnTo=) · Verified 3×: 12 cold deep-links 0 bounces; authorized create 201 no 401; expired access token → silent refresh stays on page · PASS
- FIX-03 · components/providers/query-provider.tsx (retry transient 5xx up to 3× with exponential backoff, no retry on 4xx) · Verified: detail routes load clean; nav storm across list+detail 0/19 5xx locally. Root cause of prod 503 = Render free-tier cold start (~50s); client retry now self-heals it. Recommend keep-warm ping to /api/v1/health (ops). · PASS (client mitigation) / root-cause = infra cold start
- FIX-04 · api/pilgrims/dto/create-pilgrim.dto.ts (PilgrimStatus enum now mirrors prisma DB enum), web/lib/statuses.ts (NEW single source: PILGRIM_STATUSES, INVOICE_STATUS_META, INVOICE_STATUSES, PILGRIM_ACTIVE_STATUSES), components/pilgrims/pilgrim-list.tsx (form uses shared list; +PROSPECT/BOOKED configs), components/finance/finance-view.tsx (INV map + filters from shared, all 8 states) · Verified: all 11 DB pilgrim statuses PUT 200 (was 0 working — 400/500 before); invalid→400; UI form offers only valid 11; invoice filters cover Partial/Cancelled/Void · PASS
- FIX-08 · app/(auth)/login/page.tsx (footer Privacy/Terms/Help → /privacy /terms /help + Link import), components/public/public-chrome.tsx (SOCIALS list; icons render as disabled placeholders, no href="#"), app/page.tsx (store badges marked "Soon") · Verified: login footer legal links navigate; 0 dead href=# on landing; 5 social icons = disabled placeholders · PASS. NOTE: real social handles + store URLs = pending human content.
- FIX-07 · api/reports.service.ts (added inKingdomCount = literal IN_KINGDOM), web/components/dashboard/operations-pulse.tsx (tile → inKingdomCount, matches CRM filter), api/social.service.ts getMyAccount (live _count posts/followers/following, not stale columns), web/hooks/use-social.ts (create+delete post invalidate ['social','account']) · Verified: In Kingdom tile==CRM filter (2==2); post count increments 31→32 live · PASS
- FIX-06 · api/finance.service.ts recordPayment (cumulative status derivation DRAFT/SENT→PARTIALLY_PAID→PAID from paidCents+amount; server guards: amount>0 and ≤ outstanding → 400), web/components/finance/invoice-detail.tsx PaymentsTab (NEW record-payment form: amount/method/reference, outstanding shown, settled-state handling) · Verified: partial(575)→PARTIALLY_PAID, remainder→PAID; overpay/negative→400; UI form records 201 + history updates; Partial filter returns partials · PASS. NOTE: manual/demo payments (no gateway) — per human, gateway keys pending.
- FIX-09 · web/hooks/use-api.ts (useCreatePackage/useUpdatePackage → POST/PUT /packages, which live in bookings module), web/components/packages/packages-view.tsx (NEW list + create modal: name/type/priceAdult/duration/city), web/app/(dashboard)/packages/page.tsx (NEW route), web/components/layout/sidebar.tsx (Packages link in operator My CRM) · Verified: /packages renders; sidebar link; create 201; appears in list; selectable in New Booking modal (bookings→real packages). Discrepancy: packages API is in bookings module (not /packages module); stores basePriceCents. · PASS
- FIX-05 · Verified existing code: vendor auto-provision (marketplace.service.findVendorForTenant get-or-create), listing CRUD + /marketplace/[id] detail route, full request→offer→accept→convert-to-booking chain. Fixed: marketplace-requests.service.createOffer (title default so missing title no longer 500s; price>0 guard→400). Added prisma/seed-marketplace.ts (idempotent DEMO seed: 4 vendors + 6 listings across hotel/transport/visa). Verified: full loop 5/5 (convert creates real booking); guest marketplace now shows 15 listings/5 vendors by category · PASS (code). NOTE: production demo-supply seed = one human-authorized command at deploy (`DATABASE_URL=<prod> npx ts-node prisma/seed-marketplace.ts`); real provider content = business decision.
- PHASE-2 role sweep · All 7 roles (Super Admin, Umrah Operator, Hotel, Transport, Visa, Finance, Pilgrim) × 22 routes = 154 loads · Verified: 0 crashes, 0 server 500s on happy-path navigation · PASS
- BUILD GATE · Fixed TS build errors from changes: reports overview type (+inKingdomCount), packages-view submit (return-toast → statement). Web `next build` ✓ (all routes compile), API `nest build` ✓ (dist/src/main.js). · PASS
- FIX-10 (image upload) · Backend POST /uploads exists (5MB, image-only, served at /uploads). Image entry currently via URL; drag-drop file-upload UI + object storage = deferred (needs storage keys — human). Marked ready-for-URL, upload endpoint live. · PARTIAL / BLOCKED-HUMAN (storage)
- DEPLOY · Pushed main+develop → Vercel (web) + Render (API) auto-deployed LIVE. Seeded production marketplace (4 vendors, 6 listings). Verified LIVE on umrahconnect.io + onrender.com: FIX-01 403✓, demo login✓, FIX-04 statuses✓, FIX-07 inKingdomCount✓, FIX-05 6 listings✓, FIX-06 payment 201✓; browser: guest marketplace shows listings, demo login→dashboard, /packages live. 6/6 API + 4/4 browser. · DEPLOYED
- BP-03 · Evidence: pilgrim edit via REAL browser click+selectOption → PUT 200 captured → hard reload → "Returned" persisted in row. Extended round-trip proof: booking.status 200→CONFIRMED, booking.notes 200→persisted, visa.approve 200→APPROVED, hotel.starRating 200→4, invoice.notes 200→persisted. FOUND+FIXED real defect: bookings.service.updatePackage silently dropped durationDays/price/description/dates (200-no-change) → now maps every editable field; re-proof durationDays 21 + basePriceCents 1350000 persist. Red-team: bogus status→400, missing id→404, unauth write→401 (no 5xx). · PASS
- P1-residual (Resources) · web/lib/articles.ts (NEW: 8 fully-written real articles), web/app/resources/[slug]/page.tsx (NEW article route + related + 404 on bogus), web/app/resources/page.tsx (cards → /resources/<slug>) · Evidence: 8/8 slugs 200 with real content, bogus slug 404, 0 card dead-ends · PASS
- BP-04 · components/compliance/compliance-list.tsx (regulator chips → honest PLANNED pills w/ tooltip; no green live-claim), app/(auth)/login/page.tsx ('Nusuk & SISKOPATUH-ready visa workflows', chip 'Nusuk-ready'), deleted components/social/social-feed.tsx (unused legacy file containing fabricated "Nusuk Masar — Official" post; imported nowhere) · Evidence: browser 3/3 — 0 overclaims on login, 4 PLANNED chips, 0 live-claim chips · PASS
- BP-02 · FOUND+FIXED 2 real defects: (1) hotels.service createRoom/updateRoom passed numeric floor/roomNumber raw to String-typed Prisma columns → 500 on any numeric input (now coerced); (2) createHotelBooking without hotelId hit `findFirst({id:undefined})` (matches first hotel!) then 500 on create → now 400 BadRequest. Seeded via API (idempotent audit/bp02_supply.py): 2 hotels (Al Safwah Royale Orchid, Dar Al Eiman Grand) + 2 room types + rooms; 3 vehicles (HAJ-4921/1188/7755) + 2 drivers + 2 routes (Jeddah Airport→Makkah, Makkah→Madinah). Evidence 24/24: hotel booking 201→in list→occupancy 0%→14% & booked 0→1; assignment 201→in list; transport booking 201→in list (bookings = assignment-shaped alias); visa app 201→doc add MISSING→RECEIVED persists→reject-with-reason persists ("Passport expires within 6 months") + notes persist. Red-team: no-hotelId→400, no-vehicleId→400, numeric floor→201 (was 500), bogus visa→404, unauth→401 — zero 5xx. Browser (audit/bp02_dash.js): 9/9 supply pages render non-zero real data (hotel/transport/visa dashboards, lists, bookings). · PASS
- BP-05 · FOUND+FIXED 3 real defects: (1) follow was dead code — service toggleFollow existed but NO route exposed it; UI showed Followers/Following tiles + "N followers" that could never change → added POST /social/accounts/:id/follow + isFollowing in discover/people + Follow/Following button on /discover; (2) like state died on reload — feed never returned viewer's reaction and PostCard hardcoded liked=false with double-count math (+1 on top of refetched server count) → feed now includes viewer reactions, card inits from server truth, count uses delta-vs-server; (3) groups.service threw 5 bare `Error`s (poll <2 options, empty post/comment body, invite/doc validation) → 500s now BadRequestException → 400. Evidence API 23/23 (audit/bp05_social.py): like→fresh-fetch shows likeCount 1 + viewer reaction, unlike restores 0; comment persists + count 1; follow→isFollowing true on re-fetch, target followerCount 0→1, my profile following 0→1, unfollow restores; group post/note/poll counters each +1; poll vote 201 + persisted. Red-team: follow self→400, bogus→404, react bogus→404, poll 1-option→400 (was 500), unauth→401. Browser 7/7 (audit/bp05_browser.js): like survives hard reload (heart stays filled), Follow→Following survives hard reload, group Posts/Notes/Polls counters render non-zero, marketplace (package discovery) 15 real listing links. · PASS
- PHASE-5 (a11y/UX) · globals.css (global :focus-visible outline — keyboard-only, mouse UX unchanged), 22 ad-hoc modals across 17 files (role="dialog" aria-modal="true" via uniform overlay pattern), contrast sweep 366× text-gray-400→text-gray-500 across 63 files (all on light surfaces; AA 4.8:1 vs failing 2.8:1), app/signup/page.tsx (inline-on-blur validation: per-field errors + aria-invalid + red border, input preserved; submit marks all touched; aria-labels on all inputs; role="alert" on server error), 20 guided empty states across 12 components ("No X yet — how to get started" copy, same element, no layout change) · Evidence browser 7/7 (audit/p5_a11y.js): Tab shows visible ring; signup blur shows inline error, value preserved, aria-invalid=true; New-booking modal exposes role=dialog; 0 text-gray-400 in rendered DOM; guided empties render. Regression: 9/9 dashboards still render (bp02_dash.js), web tsc clean. · PASS
- PHASE-6 / DEPLOY-2 · Gates: web `next build` ✓ (all routes), API `nest build` ✓. Pushed main (8de1a8a, 85 files) → Vercel + Render auto-deployed. Live markers verified: umrahconnect.io/resources/best-times-to-visit-rawdah 200 (new article route), POST /social/accounts/:id/follow 401-unauth (was 404 pre-deploy = route live). Prod supply seeded + proven (bp02_supply.py vs prod tenant): 24/24 — hotels 3 / rooms 5 / occupancy 0→25% / vehicles 3 / drivers 3 (FOUND+FIXED seed defect: driver DTO needs firstName/lastName not name — local runs never exercised the create branch) / routes 2 / transport+hotel bookings persist / visa checklist + reject-with-reason live. Prod social proven (bp05_social.py): 23/23 — like/comment/follow round-trips, group counters, red-team all 4xx. Per-role LIVE sign-off screenshots: 20 pages × 7 roles captured (audit/screens/) — hotel dashboard shows seeded non-zero KPIs (3 hotels, 20% occupancy, SAR 11,400 outstanding); /discover shows Follow/Following with persisted state. NOTE: www.umrahconnect.io has no DNS record (apex umrahconnect.io works) — add a www CNAME in the domain settings if the www form is wanted (human/DNS access). · PASS / DEPLOYED
- BP-06 (Visa Service Requests — full ticket workflow) · NEW `plugin_visa` models VisaServiceRequest / VisaServiceRequestNote / VisaServiceRequestEvent + enums (category ×9, priority ×4, status ×6, note visibility) in prisma/schema.prisma; NEW api module `visa-requests` (controller 15 routes, service, DTOs) reusing the existing `visa:application:*` permission set; NEW web `hooks/use-visa-requests.ts`, `components/compliance/visa-request-queue.tsx` (list + stats tiles + filters + create modal), `visa-request-detail.tsx` (workflow bar, internal-vs-public notes, timeline, assignee/priority/due-date panel, lifecycle), `visa-requests-tabs.tsx` (ticket queue is the default inbox; the pre-existing marketplace-demand list is preserved as a second tab), routes `/visa-requests` + `/visa-requests/[id]`; statuses added to `lib/statuses.ts` (single source, per FIX-04); `VISA_REQUEST` added to NotificationType + bell icon.
  Ticket numbering VSR-<year>-<seq> unique per tenant with P2002 retry. Requester-facing `GET :id/public-thread` returns PUBLIC notes only — the internal/public split is enforced server-side, not cosmetically.
  FOUND+FIXED 2 real defects: (1) every mutation returned **500 while the write had already committed** — `NotificationType.VISA_REQUEST` was added to schema.prisma but never `db push`ed, so `notification.create` threw after the ticket update landed; the client saw a failure for work that succeeded. Root cause pushed; (2) notification dispatch was fatal to the request — a side-effect fault could 5xx an already-committed state change, so `notifyAssignee` now logs loudly and swallows, mirroring the established AuditService.log pattern. (Observation, not changed: `NotificationsService.fire` is still fatal for the other modules that call it.)
  Evidence API 51/51 (audit/bp06_visa_requests.py): create→VSR-2026-00001; category/priority stored; list+persist; overdue computed from a past due date; filters category/q/overdue/unassigned; stats; assign→persists on re-fetch; OPEN→IN_PROGRESS persists; internal note + public response both visible to staff while public-thread shows the public one and **leaks no internal note**; firstResponseAt stamped by the public reply; escalate (raises priority, reason persists); resolve; close; reopen (reopenCount 1, terminal stamps cleared); 9-event ordered timeline; merged activity feed; **8 notifications delivered to a real second account** (visa.officer@alharamain.sa) with deep links; 9 audit rows. Red-team 18/18 all 4xx, zero 5xx: bogus id 404, malformed uuid 400, missing/short subject 400, bogus category/priority/visibility 400, empty note 400, terminal status via /status 400, same-status 400, reopen a live ticket 400, escalate w/o reason 400, assign to a non-tenant user 400, empty patch 400, unauth 401, double-close 400, escalate-a-closed 400.
  Evidence browser 29/29 (audit/bp06_browser.js) + interactive run: real email-first login → real clicks through create (inline validation blocks an empty subject) → internal note → public response → escalate → resolve → close → reopen → **hard reload**: reopened status, both notes with INTERNAL/PUBLIC badges, all 7 timeline event types, escalation reason, resolution and the first-response stamp all persist. Replaced 4 `window.prompt` calls with a shared `role="dialog"` ReasonModal (Phase-5 a11y parity, keyboard/SR accessible); the script asserts no native prompt fires.
  Regression: bp02_supply 24/24, bp05_social 23/23, bp02_dash 9/9, bp05_browser 7/7, p5_a11y 7/7; `next build` ✓, `nest build` ✓. · PASS
- AUDIT-HARNESS repair · 17 browser scripts logged in via a demo persona tile, but the recovery commit (`9b6d12e`) made "Sign In to Workspace" the default login tab, so every one of them timed out waiting for the tile — the project's whole browser-proof harness was dead on a fresh machine. Added a tolerant "click Quick Demo Access first" shim after each `/login` navigation (booking_ui, bp02_dash, bp03_persist, bp05_browser, final_screens, fix02_debug, fix02_session, fix03_nav, fix06_dbg, fix06_form, fix06_payment, fix09_packages, group_diag, operator_deep, operator_smoke, p5_a11y, role_sweep). Verified restored: bp05_browser 7/7, bp02_dash 9/9, p5_a11y 7/7. NOTE: running `next build` while `next dev` is live overwrites `.next` and 500s the dev server — restart web after a build gate. · PASS
- BP-07 (Super Admin — Tenants D & Users E) · api `admin.service.ts` + NEW `dto/admin.dto.ts` (enum-validated status DTOs), `admin.controller.ts` (+2 CSV export routes, actor threaded into every mutation), `admin.module.ts` (+AuditModule); web NEW `components/ui/confirm-dialog.tsx` (shared role="dialog" confirm with optional type-to-confirm), NEW `components/admin/admin-tenant-detail.tsx` + route `/admin-tenants/[id]`, rewritten `admin-tenants-view.tsx` and `admin-users-view.tsx`, extended `hooks/use-admin.ts` (pagination, reason, blob CSV download), tenant/user status vocabularies moved into `lib/statuses.ts` (FIX-04 convention).
  FOUND+FIXED 9 real defects on the platform's highest-privilege surface. Status-enum drift (the FIX-04 class, still present here): the tenant control offered **INACTIVE** and the user control offered **SUSPENDED**, neither of which exists in TenantStatus/UserStatus — picking either 500'd, and `archiveTenant` wrote that same invalid 'INACTIVE', so **the Archive button could never have worked**. 5xx-on-bad-input: invalid tenant status, invalid user status, unknown user id, unknown role id and unknown user on role-assign all 500'd instead of 4xx. Fake success (worse — the API lied): an empty status body returned 200 having changed nothing; force-logout on a nonexistent user returned 201 `{success:true}`; revoking a role the user never had returned 200 `{success:true}`. Cross-tenant role grants were accepted (201) and rendered as a role chip although a foreign-tenant role grants nothing at permission-check time — a misleading admin surface and a latent escalation path; now 400. **No admin mutation wrote an audit row at all** (0 rows for tenant/user/user_role) — suspend, archive, lock, grant, revoke and export are now all audited with actor, before/after state and reason. Added a self-lockout guard (suspending or archiving the tenant you are signed in to → 400; the auth guard rejects non-ACTIVE tenants, so it previously locked the operator straight out — reproduced during this work). Reactivating an archived tenant left `deletedAt` set, leaving it ACTIVE **and** archived at once; ACTIVE now clears it.
  Evidence API 55/55 (audit/bp07_admin.py): suspend→persist→filter→search→reactivate; suspension proven **enforced** (that tenant's user gets 401 'Tenant is not active', and regains 200 after reactivation); archive→CHURNED+deletedAt→reactivate clears it; user LOCKED round-trip; force-logout revokes 3 real sessions, the revoked refresh token is then rejected, and a second call honestly reports 0; role grant→shows on user→revoke→gone; tenants CSV (header + one row per tenant, attachment filename) and filtered users CSV; every mutation audited incl. DATA_EXPORT. Red-team 21/21 all 4xx, zero 5xx.
  Evidence browser 34/34 (audit/bp07_browser.js) + interactive run: filters render exactly the 7 real tenant statuses and 4 real user statuses, and **0** occurrences of the invalid INACTIVE/SUSPENDED options remain; every privileged action goes through a `role="dialog"` confirmation naming the real consequence; archive is gated by typing the tenant slug (disabled → still disabled on a partial match → enabled on exact → Cancel mutates nothing, DB-verified); suspension and user lock both SURVIVE hard reload; tenant detail renders users/KYC/at-a-glance plus a per-tenant audit trail naming the actor; force-logout reports the real session count; CSV arrives as a genuine browser download; no native confirm()/prompt() anywhere.
  Regression: bp02_supply 24/24, bp05_social 23/23, bp06_visa_requests 51/51, bp05_browser 7/7; `next build` ✓, `nest build` ✓. · PASS
- AUDIT-HARNESS re-runnability · `bp05_browser.js` clicked Follow and never restored it, so a later `bp05_social.py` found every discoverable account already followed and died on an IndexError — the browser proof was silently poisoning the API proof. The browser script now unfollows what it followed, and `bp05_social.py` self-heals by unfollowing one account when none are free. Verified by running social → browser → social back-to-back: 23/23, 7/7, 23/23. · PASS
- DEPLOY-3 (partial) · Pushed `726eeb6` to main after both build gates. Vercel is live: umrahconnect.io 200 and the new `/visa-requests` route serves 200. **Render API is NOT responding** — https://umrah-connect-api.onrender.com returns no bytes at all on three attempts (120 s each); DNS, TLS and HTTP/2 all connect, the request is sent and the service never answers, which is beyond the documented ~50 s free-tier cold start. Ruled out my change as the cause: replayed production's exact upgrade path locally (old committed schema pushed to a scratch DB, then `prisma db push --skip-generate --accept-data-loss` exactly as the Dockerfile CMD runs it) → clean sync in 147 ms, and booted the built artifact (`node dist/src/main.js`) against that upgraded DB → healthy in 2 s with `/visa-requests` and `/admin/tenants/export` both mapped. **HUMAN ITEM: check the Render dashboard for `srv-d94peplckfvc73adlr9g`** (failed deploy, suspended free instance, or exhausted free hours). Not verified live until the service answers. · BLOCKED-HUMAN
